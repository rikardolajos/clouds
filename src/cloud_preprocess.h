#ifndef CLOUD_PREPROCESS_H
#define CLOUD_PREPROCESS_H

#include "texture.h"

int cloud_preprocess1(Texture* cloud_structure, Texture source);
int cloud_preprocess2(Texture* cloud_structure, Texture source);

#endif